# wardrobe: .
# accessory: chick
# made for: si applica su una xubuntu installazione minima
#           lasciando cosi lo spazione per la scelta dei
#           pacchetti da installare.
# note:     al momento non installa nulla, mera configurazione
#           del desktop, repo di eggs, vscode 
# packages: mi viene in mente gcompris
---
name: chicks
description: >-
  desktop xfce4 per scuola primaria
author: artisan
release: 0.0.2
distributions:
  - bullseye
  - jammy
sequence:
  packages:
    # jush a bit educational gtk stuffs
    #- drgeo # # not found interactive geometry
    - gcompris
    - gelemental
    #- gnome-chemistry-utils # not found # gtk widgets and small programs for chemistry
    - granule # Leitner system flashcard utiltiy (text only)
    - klavaro # touch typing
    - littlewizard # development environment for children
    #- rurple-ng # not found # educational tool to help students learn the Python programming language (comes with 48 lessons)
    - solfege #  ear training
    - stellarium
    - tuxtype # TypingTutor - touch typing plugin for gedit
    - wordplay #  anagrams

    # jush a bit educational kde stuffs
    # keduca  # non esiste nelle repo
    #- kalzium
    #- kanagram
    #- kbruch
    #- khangman
    #- kig
    #- kmplot
    # kpercentage  # non esiste nelle repo
    #- kstars
    #- ktouch
    #- kturtle
    # kverbos # non esiste nelle repo
    # kvoctrain # non esiste nelle repo
    #- kwordquiz    
  accessories:
    - base # per la repository di eggs
    - eggs-dev # al momento serve a me
customize:
  dirs: true
  scripts:
    - ../../scripts/config_desktop_link.sh
    - ../../scripts/config_lightdm.sh
    #
    # insert command g4artisan
    #
    - rm -f /usr/local/bin/g4*
    - curl -fsSL "https://raw.githubusercontent.com/pieroproietti/penguins-eggs/master/g4/g4artisan" -o /usr/local/bin/g4artisan
    - chmod +x /usr/local/bin/g4artisan
    - curl -fsSL "https://raw.githubusercontent.com/pieroproietti/penguins-eggs/master/g4/g4clone" -o /usr/local/bin/g4clone
    - chmod +x /usr/local/bin/g4clone
    - curl -fsSL "https://raw.githubusercontent.com/pieroproietti/penguins-eggs/master/g4/g4passwd" -o /usr/local/bin/g4passwd
    - chmod +x /usr/local/bin/g4passwd

  hostname: true
#reboot: true 